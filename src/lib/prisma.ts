// Demo mode: Prisma is not used. All data comes from src/data/*.ts via mock-db.ts
// This file is kept as a stub so existing imports don't break if any are missed.
export const prisma = null as never;
